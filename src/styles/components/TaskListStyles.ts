import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  emptyState: {
    alignItems: 'center',
    backgroundColor: '#ffffff',
    borderColor: '#e4e7ec',
    borderRadius: 16,
    borderWidth: 1,
    padding: 24,
  },
  emptyStateTitle: {
    color: '#16213e',
    fontSize: 18,
    fontWeight: '700',
    marginBottom: 8,
  },
  emptyStateText: {
    color: '#667085',
    fontSize: 14,
    lineHeight: 20,
    textAlign: 'center',
  },
});
